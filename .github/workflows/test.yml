name: CML
on: [push]

jobs:
  job1:
    name: Test Job 1
    runs-on: ubuntu-latest
    container: docker://ghcr.io/iterative/cml:0-dvc2-base1
    
    steps:           
      - name: Check out repository code
        uses: actions/checkout@v3
        
      - name: push summary
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event." > report.md
          cml send-comment report.md
          
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}." >> report.md
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner." >> report.md
      - name: check current pwd
        run: pwd
        
      - name: List files in the workdir
        run: |
          cd ./Module\ 2.\ Data\ governance
          ls
        
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
    
